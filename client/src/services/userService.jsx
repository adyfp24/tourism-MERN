import axios from 'axios';

const userService = () => {

  const registService = async () => {

  };

  const loginService = async () => {

  };

  const logoutService = async () => {
    
  }; 

  return { registService, loginService, logoutService};
}

export default userService